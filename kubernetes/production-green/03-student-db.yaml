apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: student-db-pvc
  namespace: production-green
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-db
  namespace: production-green
  labels:
    app: student-db
    slot: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: student-db
  template:
    metadata:
      labels:
        app: student-db
        slot: green
    spec:
      containers:
        - name: student-db
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              value: students
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: student-db-storage
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: [sh,-c,"pg_isready -U postgres -d students"]
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: student-db-storage
          persistentVolumeClaim:
            claimName: student-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: student-db
  namespace: production-green
spec:
  selector:
    app: student-db
  ports:
    - port: 5432
      targetPort: 5432
